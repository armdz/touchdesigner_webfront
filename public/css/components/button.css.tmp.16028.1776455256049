.button-control {
  padding: 0;
  overflow: hidden;
}

.button-control__btn {
  width: 100%;
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 3px;
  background: var(--bg-3);
  border: none;
  border-radius: 0;
  color: var(--text);
  cursor: pointer;
  transition: background .1s, transform .05s;
}

.button-control__btn:hover:not(.button-control__btn--active) { background: #2a2a2a; }
.button-control__btn:active:not(.button-control__btn--active) { background: var(--accent); color: #000; transform: scale(.97); }
.button-control__btn--active { background: var(--accent); color: #000; }
.button-control__btn--active:hover { background: var(--accent); opacity: .85; }
.button-control__btn--active:active { transform: scale(.97); }

.button-control__text {
  font-size: 13px;
  font-weight: 500;
  pointer-events: none;
}

.button-control__btn .control__id {
  font-size: 9px;
  font-family: monospace;
  opacity: .35;
  pointer-events: none;
}
